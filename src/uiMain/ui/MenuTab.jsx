const MenuTab = ({ children }) => (
  // warning className classes are:
  <div className="flex items-center text-xs h-6 px-2 edge-corner mb-2 w-full md:w-1/2">
    {children}
  </div>
);

export default MenuTab;
